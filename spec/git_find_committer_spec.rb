require 'spec_helper'

describe GitFindCommitter do
  it 'has a version number' do
    expect(GitFindCommitter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
